/*
  範例 env.js

  使用方式 (純前端):
  1. 複製此檔案為 `env.js`（不要加入版本控制，例如加入 .gitignore）
  2. 在 `index.html` 中把 <script src="env.js"></script> 放在 <script src="config.js"></script> 之前
  3. 將下面值換成您自己的值
*/

window.APP_CONFIG = {
  // OAuth Client ID
  GOOGLE_CLIENT_ID: 'YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com',
  // Spreadsheet ID
  SPREADSHEET_ID: 'YOUR_SPREADSHEET_ID'
};
